plugins {
    id 'java'
    id 'org.springframework.boot' version '3.3.5'
    id 'io.spring.dependency-management' version '1.1.6'
    //id "net.kyori.blossom" version "2.1.0"
}

group = 'me.xap3y'
version = '1.0-beta.26'

def otlpEndpoint = 'http://0.0.0.0:4317'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(22)
    }
}

configurations {
    compileOnly {
        extendsFrom annotationProcessor
    }
}

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    implementation 'org.springframework.boot:spring-boot-starter-security'
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'net.coobird:thumbnailator:0.4.1'
    implementation 'org.springframework.boot:spring-boot-starter-actuator'
    implementation 'org.springframework.boot:spring-boot-starter-thymeleaf:3.3.5'
    implementation 'org.springframework.boot:spring-boot-starter-mail:4.0.0-M1'
    implementation 'org.springframework.boot:spring-boot-starter-websocket:3.5.4'
    implementation 'org.telegram:telegrambots-longpolling:8.3.0'
    implementation 'org.telegram:telegrambots-client:8.3.0'
    implementation 'com.discord4j:discord4j-core:3.2.7'
    implementation 'org.apache.commons:commons-compress:1.27.1'
    implementation "com.github.junrar:junrar:7.5.5"
    implementation 'software.amazon.awssdk:s3:2.33.8'
    implementation 'com.github.gotson.nightmonkeys:imageio-heif:1.1.0'
    implementation 'com.twelvemonkeys.imageio:imageio-jpeg:3.10.1'
    implementation 'io.micrometer:micrometer-core:1.16.0-M3'
    implementation 'io.micrometer:micrometer-registry-prometheus:1.16.0-M3'
    implementation 'io.micrometer:micrometer-tracing-bridge-otel:1.6.0-M3'
    implementation 'com.github.loki4j:loki-logback-appender:1.2.0'

    compileOnly 'org.projectlombok:lombok'
    runtimeOnly 'org.mariadb.jdbc:mariadb-java-client'

    annotationProcessor 'org.projectlombok:lombok'

    testImplementation 'org.springframework.boot:spring-boot-starter-test'
    testImplementation 'org.springframework.security:spring-security-test'
    testImplementation 'org.assertj:assertj-core:3.26.3'

    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}

tasks.named('test') {
    useJUnitPlatform()
}
/*

sourceSets {
    main {
        blossom {
            javaSources {
                property("version", version.toString())
            }
        }
    }
}*/

bootRun {
    jvmArgs = [
            "-javaagent:${projectDir}/opentelemetry-javaagent.jar",
            "-Dotel.service.name=space",
            "-Dotel.exporter.otlp.endpoint=${otlpEndpoint}",
            "-Dotel.exporter.otlp.protocol=grpc",
            "-Dotel.metrics.exporter=none",
            "-Dotel.logs.exporter=none",
            "-Dotel.instrumentation.logging.mdcs.enabled=true",
            "-Dotel.log.level=warn"
    ]
}
